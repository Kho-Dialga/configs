#!/bin/sh

# Set as a cron job to check for new RSS entries for newsboat.

ping -q -c 1 example.org > /dev/null || exit

echo 🔃 > /tmp/newsupdate
~/.local/bin/statusbar/refblock news 6
newsboat -x reload
rm -f /tmp/newsupdate
~/.local/bin/statusbar/refblock news 6
